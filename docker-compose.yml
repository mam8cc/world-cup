services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: worldcup
      POSTGRES_PASSWORD: worldcup
      POSTGRES_DB: worldcup
    ports:
      - "5433:5432"
    volumes:
      - wcdata:/var/lib/postgresql/data

volumes:
  wcdata:
